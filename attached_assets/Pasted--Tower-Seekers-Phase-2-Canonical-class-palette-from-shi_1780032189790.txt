# Tower Seekers — Phase 2: Canonical class palette from shipped sigils

## Goal

Sample the dominant colors from each shipped `<class>-sigil.svg` and write them into a single canonical class palette in `lib/game-data`. Every later phase (silhouette tinting, badges, accents, role colors) will pull from this one source of truth instead of re-deriving colors ad-hoc per page.

## Why

- Right now, role/class colors are scattered: `#f0c040` for "Physical" in PreSelection, `#a78bfa` for "Magical", `#ff8844` for Knight in SpritePreview, all unrelated to the shipped sigil art.
- The 24 SVG sigils + silhouettes shipped on 2026-05-28 are the visual source of truth. The palette must derive from them, not from theory.
- Once this exists, the `<ClassEmblem>` component (Phase 3) can mask-tint silhouettes with the class color, and every UI accent (chips, dividers, focus rings) can pull `CLASS_PALETTE[unit.id].primary`.

## Scope

**New file only:** `lib/game-data/src/class-palette.ts`
**One export added to:** `lib/game-data/src/index.ts`

Do NOT touch any pages or components in this commit. This is pure foundation.

## Sampled colors (from shipped sigils)

The dominant fill from each `artifacts/game-client/public/assets/units/<class>-sigil.svg`:

| Class     | Primary hex | Notes                                  |
|-----------|-------------|----------------------------------------|
| knight    | `#225172`   | Deep steel blue                        |
| paladin   | `#D4A857`   | Heraldic gold (the one allowed gold)   |
| berserker | `#A83A3A`   | Crimson red                            |
| lancer    | `#3F8A5C`   | Forest green                           |
| wanderer  | `#A7C4A0`   | Pale jade-green (drifted from spec)    |
| rogue     | `#6A4A8A`   | Twilight violet                        |
| archer    | `#4A7A4A`   | Hunter green                           |
| mage      | `#4A6FB0`   | Arcane blue                            |
| cleric    | `#DACA69`   | Soft holy gold-cream                   |
| warlock   | `#9A3A8A`   | Necrotic magenta                       |
| bard      | `#9A4960`   | Rose-wine                              |
| shaman    | `#7A5A3A`   | Earthen brown-bronze                   |

These colors are the **source of truth.** Do not "correct" them toward the spec palette in `silhouette-sigil-prompts.md` — the shipped art is what's locked.

## Exact changes

### 1. Create `lib/game-data/src/class-palette.ts`

```ts
/**
 * Canonical class palette.
 *
 * Sampled from the dominant fill in each shipped <class>-sigil.svg
 * (artifacts/game-client/public/assets/units/<class>-sigil.svg) on 2026-05-29.
 *
 * The shipped sigil art is the source of truth. If a sigil is ever re-rolled,
 * re-sample and update this table. Do NOT change a class's color here without
 * also re-rolling that class's sigil.
 *
 * Use cases:
 *   - Tint masked silhouettes (CSS mask-image + background-color).
 *   --tinted accent strips, dividers, focus rings, chips.
 *   - Role/category coloring across PreSelection, BattleSelect, GatheringHub.
 *
 * Replaces ad-hoc per-page palettes such as the role colors in PreSelection
 * (#f0c040 / #a78bfa) and SpritePreview's bespoke per-class colors.
 */

export interface ClassColors {
  /** Dominant sigil hex; the canonical "this class" color. */
  primary: string;
  /** Muted variant for backgrounds, low-emphasis chips. ~22% alpha primary. */
  primarySoft: string;
  /** Border / outline variant. ~55% alpha primary. */
  primaryOutline: string;
}

function softAlpha(hex: string, alpha: number): string {
  // hex is "#rrggbb"
  const r = parseInt(hex.slice(1, 3), 16);
  const g = parseInt(hex.slice(3, 5), 16);
  const b = parseInt(hex.slice(5, 7), 16);
  return `rgba(${r}, ${g}, ${b}, ${alpha})`;
}

function makeEntry(primary: string): ClassColors {
  return {
    primary,
    primarySoft: softAlpha(primary, 0.22),
    primaryOutline: softAlpha(primary, 0.55),
  };
}

export const CLASS_PALETTE: Record<string, ClassColors> = {
  knight:    makeEntry("#225172"),
  paladin:   makeEntry("#D4A857"),
  berserker: makeEntry("#A83A3A"),
  lancer:    makeEntry("#3F8A5C"),
  wanderer:  makeEntry("#A7C4A0"),
  rogue:     makeEntry("#6A4A8A"),
  archer:    makeEntry("#4A7A4A"),
  mage:      makeEntry("#4A6FB0"),
  cleric:    makeEntry("#DACA69"),
  warlock:   makeEntry("#9A3A8A"),
  bard:      makeEntry("#9A4960"),
  shaman:    makeEntry("#7A5A3A"),
};

/**
 * Safe lookup. Falls back to a neutral pearl color if a class id is unknown.
 * Use this in components, never read CLASS_PALETTE directly with a dynamic key.
 */
export function getClassColors(classId: string): ClassColors {
  return CLASS_PALETTE[classId] ?? {
    primary: "#dde2ec",
    primarySoft: "rgba(221, 226, 236, 0.22)",
    primaryOutline: "rgba(221, 226, 236, 0.55)",
  };
}
```

### 2. Re-export from `lib/game-data/src/index.ts`

Add this line to the existing `lib/game-data/src/index.ts` (do not remove the existing exports):

```ts
export * from "./class-palette.js";
```

The file should end up looking like:

```ts
export * from "./types.js";
export * from "./units.js";
export * from "./class-palette.js";
```

## Acceptance checks

1. `lib/game-data/src/class-palette.ts` exists with all 12 class entries.
2. `lib/game-data/src/index.ts` re-exports `class-palette.js`.
3. The package builds (`pnpm -F @workspace/game-data build`, or whatever the existing build command is — match what already passes for `units.ts`).
4. From a downstream import (test in any client file, then revert): `import { CLASS_PALETTE, getClassColors } from "@workspace/game-data"` resolves and `getClassColors("knight").primary === "#225172"` returns true.
5. No existing files modified except `index.ts`.

## Commit message

```
feat(palette): canonical class palette sampled from shipped sigils

- Add lib/game-data/src/class-palette.ts with CLASS_PALETTE + getClassColors()
- 12 entries, primary hex sampled from each <class>-sigil.svg on 2026-05-29
- Each entry exposes primary / primarySoft (22% alpha) / primaryOutline (55% alpha)
- Re-exported from @workspace/game-data
- Foundation for upcoming ClassEmblem component (silhouette + sigil composition)
```